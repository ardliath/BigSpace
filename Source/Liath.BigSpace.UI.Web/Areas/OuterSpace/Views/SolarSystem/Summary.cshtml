@model Liath.BigSpace.UI.Web.Areas.OuterSpace.Models.SolarSystem.SolarSystemSummary

<h4>@Model.Name</h4>

<table>
    @foreach (var ship in @Model.Ships)
    {
        <tr>
            <td>@ship.Name</td>
        </tr>
    }
</table>